# Prompt Genomes

Prompt genomes are stored as YAML under:

`prompts/<domain>/<toolchain>/<genome_id>.yaml`

These files are intended to be diffable and versioned in-repo.

Related runtime outputs live under `.cyntra/` (ignored by git).

