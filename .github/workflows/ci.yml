# Main CI workflow for pull requests and pushes
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  PYTHON_VERSION: "3.11"
  NODE_VERSION: "24"
  BUN_VERSION: "1.1.24"

jobs:
  # ============================================
  # Python (cyntra-kernel)
  # ============================================
  python-lint:
    name: Python Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        working-directory: cyntra-kernel
        run: pip install -e ".[dev]"

      - name: Ruff lint
        working-directory: cyntra-kernel
        run: ruff check src/

      - name: Ruff format check
        working-directory: cyntra-kernel
        run: ruff format --check src/

      - name: Mypy type check
        working-directory: cyntra-kernel
        run: mypy src/cyntra

  python-test:
    name: Python Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        working-directory: cyntra-kernel
        run: pip install -e ".[dev]"

      - name: Run tests with coverage
        working-directory: cyntra-kernel
        run: pytest --cov=src/cyntra --cov-report=xml --cov-report=term-missing

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: cyntra-kernel/coverage.xml
          flags: python
          fail_ci_if_error: false

  # ============================================
  # TypeScript/React (apps + packages)
  # ============================================
  typescript-check:
    name: TypeScript & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install dependencies
        run: bun install

      - name: TypeScript check (desktop)
        working-directory: apps/glia-fab-desktop
        run: bunx tsc --noEmit

      - name: TypeScript check (ui)
        working-directory: packages/ui
        run: bunx tsc --noEmit

      - name: ESLint (desktop)
        working-directory: apps/glia-fab-desktop
        run: bunx eslint src/ --max-warnings=0

      - name: ESLint (ui)
        working-directory: packages/ui
        run: bunx eslint src/ --max-warnings=0

  typescript-test:
    name: TypeScript Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install dependencies
        run: bun install

      - name: Run tests (desktop)
        working-directory: apps/glia-fab-desktop
        run: bun test --coverage

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: apps/glia-fab-desktop/coverage/lcov.info
          flags: typescript
          fail_ci_if_error: false

  # ============================================
  # Rust (Tauri backend)
  # ============================================
  rust-check:
    name: Rust Check & Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Set up Rust
        uses: dtolnay/rust-action@stable
        with:
          components: clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/glia-fab-desktop/src-tauri

      - name: Cargo check
        working-directory: apps/glia-fab-desktop/src-tauri
        run: cargo check

      - name: Cargo clippy
        working-directory: apps/glia-fab-desktop/src-tauri
        run: cargo clippy -- -D warnings

  # ============================================
  # Build verification
  # ============================================
  build-desktop:
    name: Build Desktop App
    runs-on: ubuntu-latest
    needs: [typescript-check, rust-check]
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Set up Rust
        uses: dtolnay/rust-action@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/glia-fab-desktop/src-tauri

      - name: Install dependencies
        run: bun install

      - name: Build frontend
        working-directory: apps/glia-fab-desktop
        run: bun run build

      - name: Build Tauri (dry run)
        working-directory: apps/glia-fab-desktop
        run: bunx tauri build --ci
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""
