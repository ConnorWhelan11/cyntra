[package]
name = "ai-core"
description = "Deterministic, engine-agnostic AI kernel primitives"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true, optional = true }

[features]
serde = ["dep:serde"]
